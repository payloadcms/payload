export type ListFeatureServerProps = {
  /**
   * When `true`, enforces strict indentation rules for list items, ensuring consistent structure.
   *
   * When `false`, indentation is more flexible. A list-item can have multiple levels of indentation
   * greater than the previous one.
   * @default 'false'
   */
  hasStrictIndent?: boolean
}
