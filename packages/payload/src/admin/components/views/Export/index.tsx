import React, { useEffect, useState } from 'react'
import { SanitizedCollectionConfig } from '../../../../exports/types'

const Export = ({ data }: { data: Array<SanitizedCollectionConfig> }) => {
  return <>hi</>
}

export default Export
