/**
 * Browser-condition stub for `@payloadcms/tanstack-start/rsc`.
 *
 * Bundlers serving the client graph load this file via the `"browser"` export
 * condition in `package.json`. The real implementations rely on server-only
 * APIs and must never be evaluated in a browser context.
 *
 * Keep in sync with `serverOnlyExports['@payloadcms/tanstack-start/rsc']` in
 * `withPayload/workarounds/clientModuleResolution.ts`, which stubs the same
 * specifier earlier in resolution for the dev optimizer.
 */

export const CollectionCards = () => null
export const HierarchyTypeFieldServer = () => null
export const renderPayloadComponentOnServer = () => null
