/**
 * @deprecated Import `DefaultNavServer` from `@payloadcms/ui/elements/Nav/Server` directly.
 * This re-export is kept for backwards compatibility and uses the data-fetching server nav
 * that lives in `@payloadcms/ui` (so both Next and TanStack adapters can use it).
 */
export {
  DefaultNavServer as DefaultNav,
  type NavServerProps as NavProps,
} from '@payloadcms/ui/elements/Nav/Server'
