import { SanitizedCollectionConfig } from '../../collections/config/types';
declare const useThumbnail: (collection: SanitizedCollectionConfig, doc: Record<string, unknown>) => string | false;
export default useThumbnail;
