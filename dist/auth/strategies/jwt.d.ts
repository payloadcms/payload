import { Strategy as PassportStrategy } from 'passport-strategy';
import { Payload } from '../../payload';
declare const _default: ({ secret, config, collections }: Payload) => PassportStrategy;
export default _default;
