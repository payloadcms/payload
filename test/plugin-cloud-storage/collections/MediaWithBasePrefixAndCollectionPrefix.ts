import type { CollectionConfig } from 'payload'

export const MediaWithBasePrefixAndCollectionPrefix: CollectionConfig = {
  slug: 'media-with-base-prefix-and-collection-prefix',
  fields: [],
  upload: {
    disableLocalStorage: true,
  },
}
