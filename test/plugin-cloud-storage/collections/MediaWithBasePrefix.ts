import type { CollectionConfig } from 'payload'

export const MediaWithBasePrefix: CollectionConfig = {
  slug: 'media-with-base-prefix',
  fields: [],
  upload: {
    disableLocalStorage: true,
  },
}
