/** Minimum weighted score for a single eval case to be considered passing. */
export const SCORE_THRESHOLD = 0.6

/** Minimum fraction of cases that must pass within a dataset. */
export const ACCURACY_THRESHOLD = 0.7
