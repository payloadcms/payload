import type { CollectionConfig } from '../../../../src/collections/config/types';

const LexicalRichTextFields: CollectionConfig = {
  slug: 'lexical-rich-text-fields',
  fields: [
    {
      name: 'lexicalRichText',
      type: 'lexicalRichText',
    },
  ],
};


export default LexicalRichTextFields;
