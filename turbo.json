{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["./dist/**"],
      "dependsOn": ["^build"],
      "cache": false
    },
    "dev": {
      "cache": false
    }
  }
}
