import Text from './Text';
import Iterable from './Iterable';
import Nested from './Nested';

export default {
  text: Text,
  textarea: Text,
  number: Text,
  email: Text,
  code: Text,
  // group: Nested,
  // row: Nested,
  // array: Iterable,
  blocks: Iterable,
  checkbox: Text,
  date: Text,
  radio: Text,
  select: Text,
  relationship: Text,
  upload: Text,
  point: Text,
};
