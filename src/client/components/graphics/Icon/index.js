import React from 'react';

import { PayloadIcon } from 'payload/components';

const Icon = () => {
  return (
    <div className="icon-wrap">
      <PayloadIcon />
    </div>
  );
};

export default Icon;
